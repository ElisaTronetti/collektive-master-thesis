% Encoding
\usepackage[utf8]{inputenc}

% Change this to any language you want
\usepackage[english]{babel}
\usepackage{csquotes}

% Sets font to Times New Roman
\usepackage{times}

% Used for images positioning
\usepackage{float}

% Used to display links
\usepackage{hyperref}
\hypersetup{
    colorlinks,
    linkcolor={black!50!black},
    citecolor={blue!50!black},
    urlcolor={blue!80!black}
}

% Bibliography packages
\usepackage[sorting=none]{biblatex}

% Shows Contents and List of Figures in the table of contents
\usepackage{tocbibind}

% Math packages
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{stmaryrd}
\usepackage{amssymb}

% Algorithms.
\usepackage[ruled]{algorithm2e}

% Allows defining custom boxed/framed environments.
\usepackage[framemethod=tikz]{mdframed}

% File listings, with syntax highlighting.
\usepackage{listings}

%Control layout of itemize, enumerate, description.
\usepackage{enumitem}

% Sectioning commands for selection from various title styles.
\usepackage{titlesec}

% Required for adjusting page dimensions and margins.
\usepackage{geometry}

% Required for inputting international characters.
\usepackage[utf8]{inputenc}

% Output font encoding for international characters.
\usepackage[T1]{fontenc}

% Used for bibliography layout management
\usepackage[final]{microtype}

% Use the XCharter fonts.
\usepackage{XCharter}

% Colour control for LaTeX documents.
\usepackage{color}
\usepackage{xcolor}

% Provides hyphenatable spacing out (letterspacing), underlining, striking out, etc.
\usepackage{soul}

% Makes possible to change line spacing locally
\usepackage{setspace}

\usepackage{fancyhdr}

\usepackage[titletoc]{appendix}
